type FeatureCardProps = {
  title: string
  description: string
  imageUrl: string
}

export default function FeatureCard({
  title,
  description,
  imageUrl,
}: FeatureCardProps) {
    return (
        
    )
}
